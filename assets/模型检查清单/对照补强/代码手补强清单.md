# 代码手 · 对照补强清单

> 依据本目录两份自查清单，对 `code/` 实际状态逐项核查后整理的待补强任务。
> 标注约定：〔评分§N〕=《根据评分标准整合后的模型完善检查清单》第 N 节；〔优秀§N.N〕=《根据以往优秀模型整合得到的模型完善检查清单》第 N.N 节。
> 优先级：**P0** 错误结论/必须先修；**P1** 影响得分主体；**P2** 加分项。

---

## 0. 现状总览（核查结论）

`code/` 已能端到端运行并产出 `results/tables`、`results/figures`，整体完成度高。但存在 **1 个会导致核心结论错误的未落地修订**，以及若干一致性/合理性缺口。

| 模块 | 状态 |
|------|------|
| `q1/run_q1.py` | ✅ 可运行；🔴 **未实现 2026-05-30b 修订（附件2 独立拟合）**，仍复用附件1 (α,β) |
| `q2/run_q2.py` | ✅ 产出 HI/阶段表；⚠️ 线性 Wiener 拟合 HI 出现 **负 R²（-70）** |
| `q3/run_q3.py` | ✅ 产出迁移/预警；数值依赖 Q1，须随 Q1 修订重跑 |
| `sensitivity/` | ✅ 已联动 |
| `results/tables` | ✅ 齐全；⚠️ 与论文部分数值已不一致 |
| `tests/` | ⬜ 空（无回归测试） |

---

## 1. 🔴 P0 — 落地"附件2 独立拟合 (α_T, β_T)"（最高优先级）

〔评分§6 参数估计〕〔评分§7 预测〕〔优秀§11.1 公式—代码—结果一致〕〔优秀§12.5 结果脱离常识〕〔优秀§12.9 代码与正文不一致〕

**问题**：建模手在 [反馈回复_2026-05-30b.md](../../../model/反馈回复_2026-05-30b.md) 与 [Q1_模型.md §5](../../../model/formulation/Q1_模型.md) 已明确：**附件2 是退化更快的独立轮子（β_T≈7.2e-4≈2β₁），模型 A 与模型 B 必须用附件2 自身拟合的 (α_T,β_T)，严禁复用附件1**。但当前代码 [run_q1.py:331-350](../../../code/src/q1/run_q1.py#L331-L350) 仍：

```python
res_a = fit_model_a(t1, i1)                                    # 只拟合附件1
rul_a = predict_rul_model_a(t2[-1], res_a["alpha"], res_a["beta"])   # ← 用附件1 α,β 算附件2
res_b = fit_wiener_lambda(t2, i2, res_a["alpha"], res_a["beta"])     # ← L344 注释"复用附件1"
```

**后果（已写进 `Q1_summary.json`、并被论文引用）**：RUL_A=1548.5、RUL_B=1000.5（A/B 偏差 35%，应 <2%）、σ_B²=0.434（应为噪声量级 ~1e-4）、附件2 R²=0.673（应 ≈0.999）。**RUL 被高估约 3 倍**（应 ≈483 天）。

**待办**：
- 在 `main()` 新增 `res_a_t = fit_model_a(t2, i2)`，将 `rul_a`、`fit_wiener_lambda`、`predict_rul_lambda_wiener` 全部改用 `res_a_t` 的 (α_T,β_T)；删除 L344"复用附件1"注释。
- 保留 `res_a`（附件1 全寿命）用于附件1 检验/阶段速率/Score；新增落盘 `Q1_theta_A_att2.csv`。
- 重跑 `run_q1.py` 并验收：`RUL_A≈RUL_B≈483`、`σ_B²~1e-4`、附件2 `R²≈0.999`、A/B 偏差 `<2%`（[Q1_模型.md §5.3 诊断锚点](../../../model/formulation/Q1_模型.md)）。
- **级联重跑** `run_q3.py`、`run_all_sensitivity.py`、绘图脚本，刷新全部 `results/`。
- 复核 PHM Score（[结果不合理性总结.md 问题5](../../../code/结果不合理性总结.md)）：修复后 Δ 量级正常，Score 应从 22025 降到 O(1)~O(10)；确认数值护栏 clip(±130)、S_max 仍在。

## 2. P1 — Q2 轴承退化模型负 R²

〔优秀§3.1 指标与目标匹配〕〔优秀§12.4 线性假设〕〔优秀§12.5 脱离常识〕〔优秀§7.5 局部误差〕

- `5_results.tex` 表 q2_hi_solution 显示线性 Wiener 拟合 HI：Bearing3_1 **R²=-70.09**、Bearing1_1 **R²=-1.12**，仅 Bearing3_5 R²=0.84。负 R² 说明"线性漂移 Wiener"对非线性（加速型）HI 严重失配。
- **待办**：与建模手确认后，二选一——(a) 对 HI 改用/并报指数退化模型（[Q2_模型.md §3.5.2](../../../model/formulation/Q2_模型.md)）的拟合优度作为主报指标；(b) 保留线性 Wiener 但在输出中同时给指数模型 R²，并让论文解释负 R² 来源。至少不能让"R²=-70 的退化模型"作为正式结果裸露。

## 3. P1 — 结果与论文/JSON 数值再同步

〔评分§14〕〔优秀§11.1、§12.8〕

- 论文表 q3_tl_solution 写 95% CI `[653.6, 1426.3]`，但当前 [Q3_summary.json](../../../code/results/tables/Q3_summary.json) 为 `[652.2, 1428.3]`——**论文数值非最新一次运行**。第 1 节修订后所有数会再变；重跑后须导出一份"权威结果"供论文手回填，并在 [一致性核查表.md](../../../paper/一致性核查表.md) 勾选"数值与 results/tables 对齐"。
- 建议：让 `run_all.py` 末尾生成一个汇总 `results/tables/_paper_numbers.json/csv`，论文表只引这一个源，杜绝手抄漂移〔优秀§6.3 参数一致〕。

## 4. P1 — 可复现性与原始数据落位

〔评分§14〕〔优秀§2.5、§11.6〕

- `6_sensitivity.tex` 不足部分自述"Q2 原始轴承数据在当前代码默认目录中缺失，本文使用已生成的 HI 结果表完成迁移验证"。但 `code/data/raw/XJTU-SY_Bearing_Datasets/` 下确有 CSV。**待办**：确认 `run_q2.py` 能从现有原始数据**重新生成** `Q2_HI_*.csv`/`Q2_top_features.*`（而非依赖已缓存结果）；若确需大数据则在 README 写明下载与放置路径，保证 `run_all.py` 一键复现〔评分§14；优秀§11.6 附录可复现〕。

## 5. P1 — 代码注释/docstring 与实际模型对齐

〔优秀§11.1、§12.9〕

- [run_q1.py:1-8](../../../code/src/q1/run_q1.py#L1-L8) docstring 仍写"模型B: Wiener 漂移过程 X(t)=X0+μt+σB(t), MLE 闭式解"——这是**已弃用的对数 Wiener**，与实际实现的 Λ-时间 Wiener（`fit_wiener_lambda`、`D(t)=Λ(t)+σ_B·B(Λ)`）不符。更新 docstring 及任何残留的 `μ/X(t)` 命名，避免附录代码自相矛盾。

## 6. P2 — 回归测试与边界检验代码

〔优秀§7.1、§7.8、§12.6〕

- `code/tests/` 为空。按 [边界检验设计.md](../../../model/边界检验设计.md) 落地最少几条断言：
  - Q1-T5 附件2 模型 A/B 一致性（修订后 `<15%`，目标 `<2%`）；
  - Q1-T2 `σ_B→0` 时 RUL CI 宽度→0、点估计=模型A；
  - Q1-T1 `β→0` 退化为线性；
  - Q3 预警偏序：`S=F ⇒ L≥L2`、`S=H ⇒ L≤L1`（[Q3_模型.md §5.3](../../../model/formulation/Q3_模型.md)，代码须断言）；
  - IG 的 `_ig_cdf/_ig_ppf` 数值一致性。

---

## 7. 执行顺序（关键路径）

1. **P0 第 1 节** → 重跑 q1 → q3 → sensitivity → 绘图 → 导出权威结果 JSON；
2. **P1 第 2 节** Q2 负 R² 处理（与建模手同步）；
3. **P1 第 3/4/5 节** 数值同步、原始数据复现、注释对齐；
4. **P2 第 6 节** 回归测试。

> 每完成一项，更新 [结果不合理性总结.md](../../../code/结果不合理性总结.md) 的真实状态（当前问题4/5 标 ⚠️，修订后应转 ✅），并通知论文手回填数值。
