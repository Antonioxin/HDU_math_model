"""集中存放数值常量。

所有出现在公式与代码中的固定参数都应在此声明，与 `model/参数总表.md` 一一对应。
"""
from __future__ import annotations

# ── 题目给定 ────────────────────────────────────────────────────────────
# 例：
# N_CENTERS = 92            # 分拣中心数
# GROWTH_RATE = 0.20        # 年货量增长率

# ── 派生 / 估计 ────────────────────────────────────────────────────────

# ── 模型超参数（默认值，敏感性扫描时覆盖） ────────────────────────────
# 例：
# EWMA_ALPHA = 0.9
# EWMA_WINDOW = 14
